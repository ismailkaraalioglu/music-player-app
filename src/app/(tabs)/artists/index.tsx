import { Text, View } from "react-native";

const ArtistsScreen = () => {
  return (
    <View className="flex-1 bg-black">
      <Text className="text-xl text-black">Artists Screen</Text>
    </View>
  );
};

export default ArtistsScreen;
