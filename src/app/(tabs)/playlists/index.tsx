import { Text, View } from "react-native";

const PlaylistsScreen = () => {
  return (
    <View className="flex-1 bg-black">
      <Text className="text-xl text-black">Playlists Screen</Text>
    </View>
  );
};

export default PlaylistsScreen;
