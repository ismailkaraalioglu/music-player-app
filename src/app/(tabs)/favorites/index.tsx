import { Text, View } from "react-native";

const FavoritesScreen = () => {
  return (
    <View className="flex-1 bg-black">
      <Text className="text-xl text-black">Favorites Screen</Text>
    </View>
  );
};

export default FavoritesScreen;
